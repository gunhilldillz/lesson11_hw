source 'https://rubygems.org'

gem 'sinatra' # our web application library
gem 'activerecord' # the ORM that wil talk to the DB
gem 'sinatra-activerecord' # lib that helps sinatra use activerve record
gem 'sqlite3' # the ADAPTER for the SQLite databse
gem 'rake' # an unrelated lib for running some commands

